import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  mainView: {
    flex: 1,
    marginTop: 8,
  },
  imageView: {
    height: 270,
    borderRadius: 18,
    resizeMode: 'cover',
  },
});
export default styles;
